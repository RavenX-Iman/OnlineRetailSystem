package gui;

import java.awt.Color;

public class ModernColors {
    // Main background color for panels and viewports (light cool gray)
    public static final Color BACKGROUND = new Color(245, 247, 250);

    // Primary accent color for headers, buttons, and highlights (modern blue)
    public static final Color PRIMARY = new Color(40, 120, 220);

    // Border color for fields and panels (subtle gray)
    public static final Color BORDER = new Color(200, 200, 200);

    // Primary text color for labels and main content (dark gray)
    public static final Color TEXT_PRIMARY = new Color(30, 30, 30);

    // Secondary text color for subtitles and descriptions (lighter gray)
    public static final Color TEXT_SECONDARY = new Color(100, 100, 100);

    // Success color for validations and positive feedback (green)
    public static final Color SUCCESS = new Color(46, 204, 113);

    // Error color for invalid inputs and errors (red)
    public static final Color ERROR = new Color(231, 76, 60);

    // Warning color for alerts and pending actions (yellow)
    public static final Color WARNING = new Color(241, 196, 15);

    // Info color for informational elements (light blue)
    public static final Color INFO = new Color(52, 152, 219);

    // Selection background color for tables and lists (light blue-gray)
    public static final Color SELECTION_BACKGROUND = new Color(240, 245, 255);

    // White background for cards and form panels
    public static final Color WHITE = Color.WHITE;

    // Light hover color for buttons (slightly darker primary)
    public static final Color PRIMARY_HOVER = PRIMARY.darker();
}